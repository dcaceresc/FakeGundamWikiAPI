@using FakeGundamWikiAPI.Areas.Maintainer.Models.ExampleTypes
@model IList<ExampleTypeDto>
@{
    Layout = "_Layout";
    ViewData["Title"] = "ExampleTypes";
}

<div class="card mt-3">
    <div class="card-header bg-primary d-flex justify-content-between align-items-center">
        <h2>@ViewData["Title"]</h2>
        <a onclick="modalOpen('@Url.Action("Create","ExampleTypes", new { area = "Maintainer"})','New ExampleType')" class="btn btn-success text-white">Create Example Type</a>
    </div>
    <div class="card-body">
        <table class="table table-striped table-bordered">
            <thead>
                <tr>
                    <th>#</th>
                    <th>Name</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.ExampleTypeId</td>
                        <td>@item.ExampleTypeName</td>
                        <td>
                            <a onclick="modalOpen('@Url.Action("Edit","ExampleTypes", new { area = "Maintainer", id = item.ExampleTypeId})','Edit ExampleType')" class="btn btn-primary text-white">
                                <i class="bi bi-pencil"></i> Edit
                            </a>
                            <a asp-action="Toggle" asp-route-id="@item.ExampleTypeId" class="btn @(item.IsActive ? "btn-warning" : "btn-success") text-white">
                                <i class="bi @(item.IsActive ? "bi-pause" : "bi-play")"></i>
                               @(item.IsActive ? "Pausar" : "Reanudar")
                            </a>
                        </td>
                    </tr>
                }
        </table>
    </div>
</div>